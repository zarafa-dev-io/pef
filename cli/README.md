# CLI `pef` (Go) — lot 5, différé

Portage Go du moteur de validation, **à iso-contrat** avec le script Node/TypeScript du template (`template/tools/validate/`) : mêmes JSON Schemas (`template/schemas/`), mêmes règles, mêmes rapports.

Déclenché lorsque le modèle sera stabilisé par l'usage et que la distribution en binaire unique (NFR-3 : Windows/Linux/macOS, sans dépendance runtime) au-delà du template le justifiera. Ajoutera `pef new` et `pef init` (EF-15).
